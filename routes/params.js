const { Router } = require('express');
const router = Router();

/*const { params } = require('../controllers/params');

router.get(
    '/getparams',
    params
     
        )

module.exports = router    */    